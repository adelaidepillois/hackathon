import styles from './game.module.css'

export default async function Page() {
    
    return (
        <div className={`${styles['bg-custom-gradient']} ${styles['min-h-screen']}`}>test</div>
    )
}